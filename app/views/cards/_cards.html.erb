<div class="results">
    <% results.each do |k,v| %>
      <h1><%= k %></h1>
      <% v.each do |card| %>
        <%= link_to image_tag(card.image_url, class: 'card_image'), assign_card_path(slot, card), method: :post %>
      <% end %>
    <% end %>
</div>
