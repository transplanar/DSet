<%# unless slots.empty? %>
  <!-- <%= react_component('CardSlot', image_url: slots.first[:image_url], slot: slots.first) %> -->
  <%# slots.each do |slot| %>
    <%#= react_component('CardSlot', image_url: slot.image_url, url: slot_path(slot)) %>
  <%# end %>
<%# end %>
